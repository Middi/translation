const express=require("express"),path=require("path"),app=express(),mongoose=require("mongoose"),bodyParser=require("body-parser");var port=process.env.PORT||3e3;mongoose.connect("mongodb://localhost/translation"),app.set("view engine","pug"),app.use(express.static("public")),app.use(bodyParser.urlencoded({extended:!0})),app.use(express.static(path.join(__dirname,"public")));var transSchema=new mongoose.Schema({polish:String,english:String,phonetic:String,category:String},{collection:"polish"}),Trans=mongoose.model("polish",transSchema);app.get("/",function(e,o){Trans.find(function(e,n){o.render("index",{data:n})})}),app.listen(port,function(){console.log("server started on port 3000")});